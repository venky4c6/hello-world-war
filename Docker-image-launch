pipeline {
  agent any
    stage("docker Image") {
        steps {
            sshagent(['ubuntu']) {
                sh """
                    docker build -t tomcat .
                """
            }
        }
    }
}
