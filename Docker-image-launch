pipeline{
  agent any
  stages{
    stage("docker Image") {
        steps {
            sshagent(['ubuntu']) {
                sh """
                    docker build -t tomcat .
                """
            }
        }
    }
    }
}
